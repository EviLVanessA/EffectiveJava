1、枚举中的switch语句适合于给外部的枚举类型增加特定于常量的行为
2、何时使用枚举？
    ①每当需要一组固定常量，并且在编译时就知道其成员的时候，采用枚举。
    ②枚举中的常量集并不一定要始终保持不变
    ③枚举的可读性更好，更加安全，功能也更加强大
    ④如果多个枚举常量同时共享相同的行为时，则需要考虑策略枚举
3、①永远不要根据枚举的序数导出与他关联的值，而是要将它保存在一个实例域中
    ②最好不要用序数来索引数组，而要使用EnumMap
    ③无法编写可扩展的枚举类型，但可以通过编写接口以及实现该接口的基础枚举类型来对它进行模拟
4、标记接口定义的类型是由被标记类的实例实现的；标记注解则没有定义这样的类型